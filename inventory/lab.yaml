# Network Lab Inventory — test environment (4 devices)
# Credentials: set NETWORK_USERNAME and NETWORK_PASSWORD env vars

devices:

  # ── Spines ────────────────────────────────────────────────────────────
  s1:
    hostname: 192.168.1.2
    platform: arista_eos
    site: lab
    role: spine

  s2:
    hostname: 192.168.1.3
    platform: arista_eos
    site: lab
    role: spine

  # ── Leafs ─────────────────────────────────────────────────────────────
  l1:
    hostname: 192.168.1.4
    platform: arista_eos
    site: lab
    role: leaf

  l2:
    hostname: 192.168.1.5
    platform: arista_eos
    site: lab
    role: leaf
